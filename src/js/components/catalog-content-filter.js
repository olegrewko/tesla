import  vars  from "../_vars.js";

if (vars.catalogFilterBlocks.length) {
vars.catalogFilterBlocks.forEach(block => {
const btn = block.querySelector(".catalog-content-filter__btn");
const inner = block.querySelector(".catalog-content-filter__inner");
const isOpenInitial = block.classList.contains(".catalog-content-filter__block--open");
});
}
